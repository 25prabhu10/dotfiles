# Get editor completions based on the configuration schema
"$schema" = 'https://starship.rs/config-schema.json'

# Timeout for commands executed by starship (in milliseconds)
command_timeout = 1000 # 500 default

# Inserts a blank line between shell prompts
#add_newline = true

[aws]
disabled = true

[battery]
disabled = true

[directory]
truncation_length = 8
truncation_symbol = "…/"
truncate_to_repo  = false
read_only = " 🔒"
